#include "ColoniaFormigas.h"
#include "Utilitarios.h"

#include <iostream>

// ============================================================================
// [EXERCÍCIO - método futuro do cronograma]
// ============================================================================
double coloniaDeFormigas(const Instancia &inst, std::vector<int> &s,
                          int numFormigas, double alfa, double beta,
                          double taxaEvaporacao, int iterMax)
{
    (void)numFormigas;
    (void)alfa;
    (void)beta;
    (void)taxaEvaporacao;
    (void)iterMax;
    std::cerr << "Colonia de Formigas: metodo ainda nao implementado.\n";
    return custo(inst, s);
}
