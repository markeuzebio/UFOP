#include "VND.h"
#include "Utilitarios.h"

#include <iostream>

// ============================================================================
// [EXERCÍCIO - método futuro do cronograma]
// ============================================================================
double vnd(const Instancia &inst, std::vector<int> &s, int r)
{
    (void)r; // evita warning de parâmetro não usado enquanto o método não é implementado
    std::cerr << "VND: metodo ainda nao implementado.\n";
    return custo(inst, s);
}
